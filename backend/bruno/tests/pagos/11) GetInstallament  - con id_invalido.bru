meta {
  name: 11) GetInstallament  - con id_invalido
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/finanzas/payments/installments/org_6B3k4dao2Wf6eGxa/5412
  body: none
  auth: inherit
}

headers {
  Accept: application/json
}

tests {
  test('Status code expected', () => {
    expect([404]).to.include(res.status);
  });

  // Validar que la respuesta es JSON válido (si no es 204)
  test('Response is JSON or empty (204)', () => {
    if (res.status !== 204) {
      expect(res.body).to.be.an('object');
    }
  });

  // Validar presencia de mensaje de error en negativos
  test('Response contains message', () => {
    expect(res.body).to.have.property('message');
    expect(res.body.message).to.be.a('string').and.not.empty;
  });

  // Log del resultado
  test('Log', () => {
    console.log('Nombre del caso: 11) GetInstallament - con id_invalido');
    console.log('Status:', res.status);
    console.log('Body:', JSON.stringify(res.body));
    expect(true).to.be.true;
  });
}

settings {
  encodeUrl: true
}

docs {
  # 📋 Caso 11: Obtener Installment - ID Inexistente

  ## 🎯 Objetivo
  Verificar que el sistema devuelve un error apropiado al intentar consultar un installment con un ID que no existe.

  ## 📍 Endpoint
  ```
  GET {{baseUrl}}/api/{{apiVersion}}/finanzas/payments/installments/org_6B3k4dao2Wf6eGxa/5412
  ```
  **Nota:** ID inexistente `5412`

  ## 📦 Request Body
  N/A (método GET no requiere body)

  ## ❌ Respuesta Esperada
  - **Status Code:** `404 Not Found`
  - **Body:** Mensaje de error indicando que el installment no existe

  ```json
  {
    "message": "Installment not found"
  }
  ```

  ## 🔗 Historia de Usuario
  **HU-FIN-1.2** - Gestión de Pagos

  ## 👤 Responsable
  Edwin

  ## 📅 Última Actualización
  2025-10-07
}
