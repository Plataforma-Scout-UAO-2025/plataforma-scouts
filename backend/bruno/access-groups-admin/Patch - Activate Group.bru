meta {
  name: Patch - Activate Group
  type: http
  seq: 13
}

patch {
  url: http://localhost:8080/api/v1/tenants/t-bruno-success-slugv04/groups/bruno-info-slugv06
  body: json
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body {
  {
    "isActive": true,
    "status": "ACTIVE"
  }
}

tests {
  test("DEBUG - response", function() {
    console.log('DEBUG RESPONSE STATUS:', res.status);
    console.log('DEBUG RESPONSE BODY:', JSON.stringify(res.body));
  });

  test("Activar grupo", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('isActive').that.equals(true);
    expect(res.body).to.have.property('status').that.equals('ACTIVE');
  });
}

docs {
  **Nombre:** Activar Grupo
  **Endpoint:** PATCH /api/v1/tenants/{tenantId}/groups/{slug}
}
